# OrgAIzer dependencies
# Install with: pip install -r requirements.txt

# Desktop UI (Qt — used by main.py on main branch)
PySide6>=6.6.0

# API backend (Tauri UI — used by main_api.py on feat/tauri-ui branch)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Packaging
pyinstaller>=6.0.0

# File system monitoring
watchdog>=4.0.0

# Machine learning
scikit-learn>=1.4.0
joblib>=1.3.0
numpy>=1.26.0

# Document text extraction
pdfplumber>=0.10.0
PyMuPDF>=1.23.0       # fitz — PDF fallback + faster extraction
python-docx>=1.1.0
python-pptx>=0.6.23   # cuttable if time is tight

# Testing
pytest>=8.0.0
